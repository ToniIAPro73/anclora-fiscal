'use client'; import { ImportCard } from './import-card'; export function ExpensesCsvCard(){return <ImportCard connectorId="expenses-csv" title="Gastos — CSV" description="Previsualiza y confirma facturas recibidas de forma idempotente." accept=".csv,text/csv" fileFieldId="expenses-csv-file" fileFieldLabel="CSV de gastos" hint="Usa la plantilla publicada. El original queda custodiado con SHA-256." renderPreviewTable={(preview)=><p>{preview.summary.records} documentos analizados. Confirma para persistirlos.</p>}/>} 
