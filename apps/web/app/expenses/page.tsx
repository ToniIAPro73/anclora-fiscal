'use client'; import { useEffect,useState } from 'react'; import Link from 'next/link'; import { PageHeader } from '@anclora/ui'; import { AppShell } from '../components/app-shell';
export default function ExpensesPage(){const[items,setItems]=useState<Array<Record<string,string>>>([]);useEffect(()=>{fetch('/api/v1/expenses',{credentials:'include'}).then(r=>r.ok?r.json():{items:[]}).then((v:{items:Array<Record<string,string>>})=>setItems(v.items));},[]);return <AppShell><PageHeader eyebrow="GASTOS" title="Facturas recibidas" actions={<Link href="/expenses/new">Alta manual</Link>}/><nav><Link href="/expenses/settings/home-office">Configurar Home Office</Link></nav>{items.length?<table><thead><tr><th>Número</th><th>Fecha</th><th>Categoría</th><th>Total</th><th>Estado</th></tr></thead><tbody>{items.map(item=><tr key={item.id}><td><Link href={`/expenses/${item.id}`}>{item.documentNumber}</Link></td><td>{item.issueDate}</td><td>{item.categoryCode}</td><td>{item.totalAmount} {item.currency}</td><td>{item.status}</td></tr>)}</tbody></table>:<p>No hay facturas recibidas.</p>}</AppShell>}
