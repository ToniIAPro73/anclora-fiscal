import Link from 'next/link'; import { PageHeader } from '@anclora/ui'; import { AppShell } from '../../components/app-shell'; export default async function ExpenseDetail({params}:{params:Promise<{id:string}>}){const{id}=await params;return <AppShell><PageHeader eyebrow="GASTOS" title="Detalle de factura recibida" backHref="/expenses"/><p>Documento custodiado con descarga autenticada y verificación SHA-256.</p><Link href={`/api/v1/expenses/${id}/attachment`}>Descargar adjunto</Link></AppShell>}
